import AdminProperties from "@/components/admin/AdminProperties";

export const metadata = { title: "Admin - Properties | Proty" };

export default function Page() {
  return <AdminProperties />;
}
